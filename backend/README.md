# EduPulse Backend

This directory is reserved for the backend services and APIs for the EduPulse application. 

Currently, the Next.js application in the `frontend/` directory handles some API routes, but this directory can be used for deploying a dedicated backend (e.g., Node.js/Express, Supabase Edge Functions, Python/FastAPI) to maintain a clean architecture.
